import { NumEpisodioPipe } from './num-episodio.pipe';

describe('NumEpisodioPipe', () => {
  it('create an instance', () => {
    const pipe = new NumEpisodioPipe();
    expect(pipe).toBeTruthy();
  });
});
